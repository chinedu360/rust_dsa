use rust_dsa::{
    linked_list_dfs::*,
    queue_dfs::{self, build_queue},
    stack_dfs::*,
    tree_dfs::*,
};
fn main() {
    // stack();
    // build_queue();
    // linked_list_fn();
    build_a_tree();
}
